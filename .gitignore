# Dependencies
node_modules/

# Build output
dist/

# Test screenshots
verify-shots/

# Editor / OS
.DS_Store
Thumbs.db
*.log

# Session / tooling state
.claude/scheduled_tasks.lock

# Cloudflare deploy artifacts
.wrangler/

# PDF OCR temp
_pdf_*/

# Temp verify/test scripts
_verify.txt
_test-brain.mjs
fuzz-*.mjs
regression-*.mjs
perf-*.mjs
_dagua_trace*.mjs
_rag*.mjs
_syn*.mjs
_stream*.mjs
_verify-*.mjs
.gstack/
public/kinh/
public/sitemap-kinh.xml

# Capacitor native projects (regenerated on each machine via `npx cap add ios|android`)
# KHÔNG commit ios/ — signing/machine-specific. Mac sẽ `npx cap add ios` lại.
ios/
android/

# App Store submission assets (generated, ~9MB, giữ local)
appstore-shots/

# Local temp / OCR / audit artifacts (KHÔNG commit)
_tmp/
_tmp_*
daozang_index*
.grok-prompt.tmp.md
docs/_fragments/

docs/_grok-*
