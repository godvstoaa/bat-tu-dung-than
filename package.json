{
  "name": "bat-tu-dung-than",
  "private": true,
  "version": "3.1.0",
  "description": "App luận Bát Tự - Dụng Thần theo Tử Bình / Phong thủy huyền học Hồng Kông",
  "type": "module",
  "scripts": {
    "dev": "vite --host ::",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node selftest.mjs",
    "deploy": "rm -rf dist && vite build && npx wrangler deploy",
    "deploy:worker": "npx wrangler deploy"
  },
  "dependencies": {
    "astronomy-engine": "^2.1.19",
    "lunar-javascript": "^1.7.3",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "playwright": "^1.61.0",
    "vite": "^7.0.0"
  }
}
